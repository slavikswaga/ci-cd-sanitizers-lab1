Building a C++ project using CmakePresets, Devcontainer, and GitHub Actions

Available configure presets:

  "clang-sanitizers" - Clang avec AddressSanitizer, UndefinedBehaviorSanitizer
  
  "clang-debug"      - Clang debug (without sanitizers)
  
  "clang-release"    - Clang release
